import { DirectivadosDirective } from './directivados.directive';

describe('DirectivadosDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivadosDirective();
    expect(directive).toBeTruthy();
  });
});
