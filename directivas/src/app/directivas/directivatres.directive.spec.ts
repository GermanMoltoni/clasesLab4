import { DirectivatresDirective } from './directivatres.directive';

describe('DirectivatresDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivatresDirective();
    expect(directive).toBeTruthy();
  });
});
