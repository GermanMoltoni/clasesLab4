import { MiBotonDirective } from './mi-boton.directive';

describe('MiBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new MiBotonDirective();
    expect(directive).toBeTruthy();
  });
});
