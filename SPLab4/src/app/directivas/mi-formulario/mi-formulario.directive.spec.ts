import { MiFormularioDirective } from './mi-formulario.directive';

describe('MiFormularioDirective', () => {
  it('should create an instance', () => {
    const directive = new MiFormularioDirective();
    expect(directive).toBeTruthy();
  });
});
